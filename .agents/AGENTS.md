# Rules

- **Do not modify local code files directly** unless the user explicitly requests to do so (e.g., "실제로 수정해줘", "코드에 적용해줘"). Instead, present the proposed code changes and logic clearly in the chat response.
